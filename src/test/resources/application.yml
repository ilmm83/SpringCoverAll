spring:
  datasource:
    url: jdbc:h2://mem:db;DB_CLOSE_DELAY=-1/for_tests
    username: h2
    password: h2
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    format-sql: true
    highlight-sql: true